__pycache__/
*.py[cod]
.pytest_cache/
.coverage

.env
.env.*
!.env.example
.venv/
.vercel/

.mimosa/
.zcode/
.locks/
.temp/
backups/
dist/
NUL

.materialized.*.staging/
.materialized.*.previous/
integrations/vendor/
distribution/files.manifest.jsonl

banco/*.db
banco/*.db-journal
banco/*.db-shm
banco/*.db-wal

# DevOps config com credenciais — o template em templates/devops.yaml serve de referência
.agents_squad/config/devops.yaml

# work/ mistura dados de múltiplos projetos/clientes (SmartSub, Arthemis, Hive-Mind,
# buzz-squad etc.) — nunca commitar isso junto num repo de cliente específico.
work/

# Artefatos SDD locais — não versionar
.superpowers/

# Lixo da máquina local — não versionar
.temp-prior-user-requests.txt
INTEGRATION_TEST_REPORT.md
.env.local
